# Gen deploy params (wbc_gen_deploy_params_v1)
#
# Install with:
#   uv run wbc-gen-export --ckpt-path .../model.pt \
#     --output-dir config/policy/gen/params
#
# Or copy from a training run's params/ after play/export.
